const sanitize = require("./sanitize");

module.exports = data => {
    let { drivers_license = {} } = data;

    //  TODO(isaiah): transform server data structure into existing API response structure
    // return sanitize();
};
